# 治理加固提案：授权窗口时钟硬前置（2026-09-15，已按 T0 复核结论修订，待批准追加）

> **状态声明**：本文是**待批准追加的提案**，不是现行治理文本。`docs/native-nx-governance.md` 为**只追加**文件、其变更须经 T0（该文 §二 第 10 条：门范围、顺序、阈值、SLO 或补丁预算变化必须回 T0）；本提案**未获批准前不生效**，批准后由后续任务按该文「追加登记（只追加不改写）」惯例将本文 §二 的登记节文本逐字文末追加，届时本提案文件保持原样、不改写。**T0 复核（2026-09-15，席次 `anthropic/claude-opus-5`）结论：可批准，但不可按原稿原样追加**，须先修订三处——§二 体例改为与既有追加节同级并新增条款 6–10、§三 补硬措辞、§四 四项开放问题回填裁定——本文即按该结论修订后的版本，提案状态仍为「待追加」。本文不改代码、不 commit、不写仓B（`~/harmonyos-signing/netbird-n1bdisc/`）。

## 一、问题陈述（2026-09-15 实际发生）

诊断授权 `AUTH-DIAG-DEVICE-VALIDATION-20260914-0002`（文件：`~/harmonyos-signing/netbird-n1bdisc/diagnostics/AUTH-DIAG-DEVICE-VALIDATION-20260914-0002.json`，含 `.sha256` sidecar）明确载有：

- `valid_from`: `2026-09-14T14:29:26+08:00`
- `valid_until`: `2026-09-15T14:29:26+08:00`
- `validity_rule`: 「上限为单会话或 24 小时（二者取小）；过期即失效，逾期操作须新签，不得顺延」

**2026-09-15 18:38–18:53（CST）** 期间，仍按该 AUTH 实际执行了设备操作（文件推送、安装、启动、uitest 点击、hilog 抓取），**超出窗口约 4 小时 9 分**。当时主机与设备时钟一致（均为 2026-09-15 18:5x CST），即越界**不是时钟漂移所致**。执行者检查了 AUTH 内容（读了窗口字段）但**未比对当前时刻与 `valid_until`**；派发方（主会话）亦未比对，且在派发稿中口头声称「在窗口内」——该声称与事实相反。事后处置：设备操作立即停止；越界产物一律标注 `out_of_authorization_window: true`、`is_evidence: false`、`not_a_gate_pass: true`。

**根因**：授权窗口有明确的 `valid_from`/`valid_until` 字段，但**流程中没有任何环节强制比对「当前时刻」**。「文件存在、内容读过」被默认等价于「仍在窗口内」；这一等价在执行者与派发方两侧同时失效，且派发方的口头声称还反向强化了执行者的不比对。窗口约束因此只在纸面上存在。

## 二、拟追加的条款文本（逐字给出，供后续 append；通过前不生效）

以下自「## 追加登记：授权窗口时钟硬前置（2026-09-15，只追加不改写）」标题起、至条款 10 止的整段文本，为批准后向 `docs/native-nx-governance.md` **文末（现 :136 之后）**追加的登记节全文（逐字）。**建议插入位置：该文文末，与既有三个文末节——「追加登记：诊断授权类别（2026-09-13）」「§四 修订（2026-09-13，唯一现行）」「追加登记：N2-H 判据设立与生效（2026-09-14）」——同级。** 体例与既有追加节一致：`##` 标题、`>` 前言另起、编号条款平铺，**不采用**原稿的整块引用格式。注意：下述标题与条款是待追加文本的逐字呈现，不是本提案自身的章节。

## 追加登记：授权窗口时钟硬前置（2026-09-15，只追加不改写）

> 本节 2026-09-15 追加：只追加、不改写上文任何内容。本文既有条款（含 §二 绑定条款、§三 pre-E8 native 物理例外、诊断授权类别登记、N2-H 判据登记）一字未动；本节为诊断授权类别及一切设备操作授权增设**时钟比对硬前置**，自 T0 批准登记时刻起**只向前生效**，不追溯。

1. **硬门前置（三时钟比对）**：任何设备操作——包括但不限于清理、卸载、撤销、force-stop、安装、点击（uitest）、抓取日志（hilog）——**开始之前**，执行者必须记录并比对：**主机当前时间、设备当前时间、该次 AUTH 的 `valid_from`/`valid_until`**；确认主机时刻与设备时刻**均**落于窗口内，方可执行第一个设备命令。比对结果必须写入当次运行记录，含主机与设备各自的具体时刻、时区、AUTH 的 `valid_from`/`valid_until` 原值与窗口内/外结论；**无比对记录即视为未比对**。
2. **派发方义务（未见比对结果不得开始）**：派发真机任务的执行者/主会话，必须在**派发稿**中写明当次 AUTH 的 `auth_id` 与 `valid_from`/`valid_until` 原文，并显式要求执行者**先回报时钟比对结果、再动手**；执行者未回报比对结果之前，任何设备命令不得发出。派发稿中关于「是否在窗口内」的一切非引用性声称不构成依据，以执行者回报的实测比对结果为准。
3. **过期即失效（窗口外产物不构成证据）**：窗口外执行产生的一切设备产物**一律不得作为证据**，必须标注 `out_of_authorization_window: true`、`is_evidence: false`、`not_a_gate_pass: true`；**不得**被任何后续 AUTH 追认，**不得**被后续测量重新引用为证据。补签的 AUTH **只对其签发时刻之后**的行为生效，不追溯覆盖签发前的越界操作；越界事实本身须如实登记，不得因补签而消失。
4. **不得顺延（依据只有两样）**：任何「口头认为还在窗口内」「距过期应该还有余量」「上次检查时还在」均**不构成依据**；唯一依据是 **AUTH 文件中的 `valid_from`/`valid_until`** 与**本次实测时钟比对结果**。AUTH `validity_rule` 的「过期即失效、逾期操作须新签、不得顺延」按字面执行。
5. **失败即停**：出现下列任一情况，一律立即停止全部设备操作并上报，**不得重试、不得绕过、不得改用其它通道**：(a) hdc 不可达；(b) 设备处于锁定/需要解锁状态；(c) 主机与设备时钟偏差超过阈值（已由 T0 裁定为 **60 秒**）。时钟偏差超阈值时，窗口比对结果不可信，属**停止条件**而非告警。
6. 比对有效期与到期即停：单次比对有效期不超过 15 分钟，超期须重比对。任何设备操作序列的预计结束时刻必须仍在窗口内；剩余窗口不足以覆盖预计耗时，不得开始。窗口在操作中途到点，立即停止并登记中断点，不得完成"手头这一步"。
7. 消费状态同为硬门：比对时须一并读取该 AUTH 的 consumed / reusable / retry_allowed；consumed:true，或 reusable:false 且已消费的，与窗口外同等处理。
8. 不可测即停止：设备时间不可读、时区未知、AUTH 文件与其 .sha256 sidecar 校验不通过、同一 AUTH 存在多份内容不一致副本——任一成立即停止上报，按窗口外处理。执行硬门所需的只读时间查询命令必须在该 AUTH 的 allowed 白名单内显式列出；未列出即不得执行设备操作（读时间不得作为"未授权但必要"的例外）。
9. 绝对时刻与单调性：一切比较在带时区的绝对时刻（epoch）上进行，禁止墙钟字符串比较。本次比对记录的主机时刻不得早于同一 AUTH 上一次已登记的 checked_at；出现倒退即视为时钟不可信，按停止条件处理。禁止为通过比对而修改主机或设备系统时间。
10. 并行与单会话：同一 AUTH 同一时刻只允许一个执行者持有设备操作权；并行执行、跨会话续用须新签。

## 三、影响范围与兼容性

- **不追溯、只向前生效**：本条款不改变既有 AUTH 的签发与消费判定——`AUTH-DIAG-DEVICE-VALIDATION-20260913-0001`（前驱，`valid_until 2026-09-14T21:45:25+08:00`，见 0002 文件 `predecessor` 字段）与 `AUTH-DIAG-DEVICE-VALIDATION-20260914-0002` 的既有状态按其自身规则维持；既有证据的标注与效力不受影响。条款自 T0 批准登记时刻起，适用于其后签发的一切 AUTH（诊断授权类别与 pair/campaign 类物理执行均适用）。
- **2026-09-15 越界事实与责任认定**：**本次执行已违反 AUTH-0002 既有 `validity_rule`**——该违规在既有规则下即成立，不是本条款创设的新义务。本条款所谓「不追溯」**仅指**不改变既有 AUTH 的消费判定与既有证据判定，**不得被读作「本次无责」**。越界处置（`out_of_authorization_window: true` / `is_evidence: false` / `not_a_gate_pass: true`、立即停止）按既有规则如实登记，不由本条款追加或削弱；补签 AUTH 对越界行为无效（条款 3）。
- **既有 AUTH 文件不改写**：比对发生在执行环节，不要求修改任何已签发 AUTH 文件（已签发文件按 `amendment_rule` 本就不得静默变更；本提案 §五 登记的 AUTH-0002 文内追加属显式登记变更，须另行授权，不在此列）；AUTH 模板（`~/harmonyos-signing/netbird-n1bdisc/diagnostics/AUTH-TEMPLATE-diagnostic-20260913.json`）是否增设时钟比对字段属仓B 变更，须另行授权，本提案不触碰。
- **需同步更新的既有文档**：
  - `docs/device-relay-enablement-runbook-20260914.md` §1 前置条件：加入「**步骤 0：授权窗口时钟比对**」（**已于 2026-09-15 随本提案一并增补**；该手册正是本次越界执行的依据文档）。
  - `docs/device-validation-plan-20260913.md`、`docs/device-validation-run1-handoff-20260914.md` 等含设备操作前置条件的后续手册，在下次修订时按同款补入步骤 0；本文不代改。
  - 派发稿模板/惯例：今后真机任务派发稿须含 `auth_id` + 窗口原文 + 「先回报比对结果再动手」要求（条款 2；记录职责按 §四 裁定 3 执行——派发方原样留档执行者回报值，禁止代填）。

## 四、原开放问题：已由 T0 裁定（2026-09-15）

原稿在此列四项「待人类/T0 决定的开放问题」。T0 复核（`anthropic/claude-opus-5`，2026-09-15）已全部裁定，悬置表述撤销，结论如下（理由保留摘要）：

1. **时钟偏差阈值与「偏差」定义——已裁定**：阈值 **60 秒**。「偏差」定义为 `skew = |host_epoch − device_epoch|`：设备时刻采样夹在主机 t0/t1 两次读数之间，取保守上界 `max(|d−t0|, |d−t1|)` 判阈；且**当前时刻距窗口边界小于（阈值+采样不确定度）时，一律判窗口外**。
2. **记录载体与字段——已裁定（双载体、主次分明）**：权威载体是运行记录 JSON 的 `clock_check`；hilog 仅作可选交叉锚，**不得作唯一记录**（本次 18:44 hilog 缓冲滚动丢标记即反证）。字段集 = 原稿字段 `auth_id, checked_at, host_time, device_time, skew_seconds, valid_from, valid_until, host_in_window, device_in_window, checked_by` + 新增 `auth_file_sha256`、`prev_checked_at`、`skew_bound_seconds`、`window_remaining_seconds`、`consumed_state`、`result(proceed|stop)`；时刻一律带时区。
3. **记录职责——已裁定（双记录、职责不对称）**：执行者实测并记录，为**唯一权威**（派发方无法代测）；派发方在派发稿写 `auth_id` 与窗口原文，并**原样留档**执行者回报值（核验副本）。两份不一致即停止；**禁止派发方代填**。
4. **超阈处置——已裁定（允许至多一次重比对，且它不是「重试」）**：`retry_allowed:false` 约束的是失败授权动作的重试；步骤 0 在任何设备变更命令之前、不消费授权、不产生产物，故重读时钟不构成授权重试。限制：仅允许修正主机对时或读取方式，**禁止改设备系统时间**；两次结果均登记；第二次仍超阈 → 停止并新签。**若第一次比对后已发出任何设备命令，则不适用，直接停止并新签。**

## 五、T0 附带要求：AUTH-0002 文内越界登记与 consumed 回填（仓B 变更，须另行授权；本提案只登记，不执行）

- 按 `amendment_rule`（已签发 AUTH 文件不得静默变更；显式登记的变更须同步校验和），T0 要求在 **`AUTH-DIAG-DEVICE-VALIDATION-20260914-0002.json` 文件内**显式**追加**：(a) 2026-09-15 越界事件登记；(b) `consumed` 状态回填；并在追加后**重算 `.sha256` sidecar**。该项属**仓B 变更**，**须另行授权**；本提案只登记该要求，**不执行**。
- **禁止删除或以重跑覆盖越界产物**；重新取证须在**新 AUTH** 下**全量重跑**，**不得与越界观测混引**。

---

*本文 2026-09-15 起草，同日按 T0 复核结论（`anthropic/claude-opus-5`）修订：§二 体例改为既有追加节同级形式并补条款 6–10、§三 补硬措辞、§四 回填四项裁定、§五 新增仓B 追加要求登记。引用的 AUTH 字段值以原稿对 `AUTH-DIAG-DEVICE-VALIDATION-20260914-0002.json` 的实读记录为准（本次修订未重读该文件，其当前 `consumed` 等字段现值待确认）；引用的治理文行号以 `docs/native-nx-governance.md` 现文（末行 :136）实读为准；全文不含任何凭据。*
