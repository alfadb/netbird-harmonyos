# 任务：为自研 NetBird 客户端 relay「单向无 WG 会话」问题索取中继侧视图（只读采集，零改动）

## 背景（一句话）

我们在开发 **HarmonyOS 平台的自研 NetBird 客户端**（非官方客户端），它已于 2026-09-15 注册到你们的生产实例（peer 主机名 `ohos-relay-1`，overlay 地址 `100.108.252.167`）并在设备侧启用了 relay：我方观测到 relay 会话全程稳定 `ready`、零重连，但上行共 **310 帧 / 42476 字节**（均值 ≈137 B/帧，算术上恰等于 **284×148B（WireGuard 握手 initiation）+ 19×HealthCheck 回显**）、下行仅 23 帧，**始终没有建立 WireGuard 会话**；观测窗口为 **A：2026-09-15 22:25:36 – 22:28:2x CST** 与 **B（主要）：2026-09-15 22:53:2x – 23:01:0x CST**（另有更早 18:38–18:55 CST 的登录烟测，peer `ohos-smoke-1`）；我们的主因假设是：官方对端因我方 signal OFFER/ANSWER 当时未携带中继地址（已在最新构建中修复）而判定 **`Relay is not supported by remote peer`**，因此从未向我方 OpenConn 建立通向我方的 lane，我方握手 initiation 因而落入其早期缓冲被丢弃——请按下表提供**中继侧视图**，用于确证或推翻这一假设。

## 需要交付的东西

| # | 事项 | 说明 |
|---|---|---|
| 1 | **中继侧该时段的会话/转发记录** | 在承载 `home.alfadb.cn:28443` 的中继宿主上，取该时段（窗口 A/B）的 relay 日志（如 `docker logs netbird-relay --since/--until`）。我们关心：**`ohos-relay-1` 与我们关心的对端是否都出现过 `peer connected`**；**是否有把我们的 Transport 帧转发给对端（或反之）的记录/计数**；有无错误或丢弃日志 |
| 2 | **中继与我们 peer 的会话存在性** | 该时段内 `ohos-relay-1` 是否在 relay 上有连接、持续多久、是否断开重连 |
| 3 | **（如可行）对端侧的判定证据** | 若你们能接触任一官方对端：在其上开 debug（约 5 分钟）并 grep 关键字 **`Relay is not supported by remote peer`**，同时 grep 我方 peer 的 key/名称；以及该对端 `netbird status -d` 中我方（`ohos-relay-1`）那一行的 **Connection type / Relay server address** |
| 4 | **时段与版本元数据** | 日志时区、中继版本、以及你们看到的任何异常 |

## 重要约束

- **只读采集**：不要修改任何配置、不要重启服务、不要改动对端设置；对端开 debug 若需要改动，请先说明影响并**用完恢复**。
- 若某个对端不能接触/不便操作：**跳过并如实说明**，不要强行。
- 日志请尽量给**原始片段**（含时间戳与时区）；若含敏感信息可先脱敏，但**不要**改写事实。
- **不要**向我们索要或让我们提供任何私钥/令牌；我们也不会提供。
- 交付方式：把日志片段与元数据打包或直接贴给我们即可。

## 备注（我们这边会做什么）

我们只把这些材料用于确证上述假设（对端从未向我方 OpenConn / 我方握手 initiation 被早期缓冲丢弃），不会据此对你们的部署做任何变更；第 3 项若不可行，仅第 1、2、4 项也很有价值。
