# Rust core build outputs (regenerated by core/build.sh)
/core/target/

# cargo-generated lockfile is committed; nothing else in core is generated
# besides target/.

# Prebuilt core .so copied into the module libs dir by build.sh before
# hvigor packaging (same layout as the spikes; not committed here).
/entry/libs/
